import React, { useEffect, useState, useContext } from "react"

const ContactPage = ({ }) => {
    useEffect(() => {

    }, [])
    return (
        <>

        </>
    )
};

export default ContactPage;
