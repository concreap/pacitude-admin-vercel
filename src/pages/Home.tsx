import React, { useEffect, useState, useContext } from "react"

const Home = ({ }) => {

    useEffect(() => {

    }, [])
    
    return (
        <>
            <h1 className="fs-38">This is a text heading</h1>
        </>
    )
};

export default Home;
